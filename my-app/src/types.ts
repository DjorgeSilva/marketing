export type ListType = {
  id: string;
  text: string;
};

export type GarantiaDataType = {
  id: string;
  fontSize: number;
  color: string;
  isUppercase: boolean;
  margin: string;
  text: string;
};

export type ContentTextType = {
  fontSize?: number;
  color: string;
  isUppercase: boolean;
  margin?: string;
  lineHeight?: string;
};

export type ImagePropType = {
  width?: string;
  margin?: string;
};

export type ButtonPropType = {
  width?: string;
  height?: string;
  margin?: string;
};

export type GenericWrapperTypes = {
  margin?: string;
};

/* export type FaceBookCommentType = {
  icon: 
} */
